<Window x:Class="LocalMark.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LocalMark.ViewModel"
        xmlns:cvt="clr-namespace:LocalMark.Converters"
        Title="LocalMark 本地数据标注工具" Height="550" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <cvt:DataTypeConverter x:Key="DataTypeConverter" />
    </Window.Resources>
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="导入文本素材" Command="{Binding ImportTextFileCommand}"
                    Width="110" Margin="0,0,5,0" />
            <Button Content="导入图片素材" Command="{Binding ImportImageFilesCommand}"
                    Width="110" Margin="0,0,15,0" />
            <Separator Width="2" Margin="0,2" />
            <Button Content="标注" Command="{Binding OpenMarkViewCommand}"
                    Width="60" Margin="15,0,15,0" FontWeight="Bold" />
            <Separator Width="2" Margin="0,2" />
            <Button Content="导出 JSON" Command="{Binding ExportToJsonCommand}"
                    Width="110" Margin="15,0,0,0" />
        </StackPanel>

        <!-- 筛选栏 -->
        <CheckBox Grid.Row="1" Content="仅显示未标注"
                  IsChecked="{Binding ShowUnmarkedOnly}" Margin="0,0,0,8" />

        <!-- 素材列表 -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Sources}"
                  SelectedItem="{Binding SelectedSource}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="类型" Width="60"
                                    Binding="{Binding DataType,
                                    Converter={StaticResource DataTypeConverter}}" />
                <DataGridTextColumn Header="内容" Binding="{Binding Content}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridCheckBoxColumn Header="已标注" Binding="{Binding IsMarked}" Width="65" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
